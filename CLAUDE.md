# CLAUDE.md — pointer file

The operating doctrine for this project now lives in **[AGENTS.md](AGENTS.md)** — the emerging cross-agent standard so both Claude Code and Codex auto-load the same instructions.

Read AGENTS.md first. Then `PROJECT_LOG.md` for setup state and `CHANGELOG.md` for version history.

(This file exists only so Claude Code, which auto-loads `CLAUDE.md`, still finds the right entry point. The actual content is in AGENTS.md.)
