[pytest]
# Project root test configuration.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Concise output by default — full tracebacks only on failure.
addopts = -ra --tb=short --strict-markers
