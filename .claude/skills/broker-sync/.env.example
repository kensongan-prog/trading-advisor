# Path to the MooMoo broker-adapter repo — copy to .env and fill in.
# broker-sync reads {MOOMOO_ROOT}/data/moomoo/*.json READ-ONLY. It never writes
# into the MooMoo repo.
MOOMOO_ROOT=/absolute/path/to/MooMoo
